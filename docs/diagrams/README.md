# Diagrams and screenshots

## Already here

- `architecture.svg` — the Azure architecture diagram, vector. Insert this one
  into PowerPoint (Insert > Pictures) — 365 handles SVG natively and it stays
  sharp at any size.
- `architecture.png` — same diagram as a raster, used in the Word report.

Insert the SVG into slides and the PNG into the Word report.
